` 쿠키 : 유효기간이 있는 키-값key-value의 쌍, 문자열 형식으로 존재, 쿠키 간에는 세미콜론으로 구분
`   ` 서버에서 요청에 대한 응답을 할 때 쿠키를 같이 보낸다
`   ` 서버->브라우저로 쿠키 전송 시에만 코드 작성으로 처리
`   ` 쿠키는 요청의 헤더(Cookie)에 포함되어 전송, 브라우저는 응답 헤더(Set-Cookie)에 따라 쿠키 저장